---
name: Rebrand App.tsx shell
status: closed
created: 2026-02-05T20:53:54Z
updated: 2026-02-05T20:59:13Z
github: https://github.com/nomubuilders/enterprise-software/issues/6
depends_on: [2, 4]
parallel: false
conflicts_with: []
---

# Task: Rebrand App.tsx shell

## Description
Update the main App.tsx component to use brand colors, integrate the Nomu logo in the top bar, add the theme toggle button, and apply the theme class to the root HTML element. Replace all slate/purple color classes with brand-aware equivalents.

## Acceptance Criteria
- [ ] Theme class (`dark` or `light`) applied to `<html>` element on mount and on toggle
- [ ] Nomu logo replaces the current app title/branding in the top bar
- [ ] Theme toggle button placed in the top-right area of the top bar
- [ ] All `bg-slate-*` classes replaced with `bg-[var(--nomu-bg)]` or `bg-[var(--nomu-surface)]`
- [ ] All `text-slate-*` classes replaced with `text-[var(--nomu-text)]` or `text-[var(--nomu-text-muted)]`
- [ ] All `border-slate-*` classes replaced with `border-[var(--nomu-border)]`
- [ ] All `bg-purple-*` / `text-purple-*` classes replaced with brand primary
- [ ] Top bar, status bar, and overall layout render correctly in both themes
- [ ] Existing functionality (modals, panels, workflow controls) unchanged

## Technical Details
- **Modified file**: `src/App.tsx`
- Import `NomuLogo` and `ThemeToggle` components
- Import `useThemeStore` to apply class to document element
- Use `useEffect` to sync theme class to `document.documentElement`
- Replace color classes systematically (search for `slate-`, `purple-`)

## Dependencies
- [ ] Task 001 (CSS variables and theme store)
- [ ] Task 002 (logo and toggle components)

## Effort Estimate
- Size: M
- Hours: 3-4
- Parallel: false (establishes the shell pattern)

## Definition of Done
- [ ] App renders in both dark and light themes
- [ ] Logo visible and properly sized
- [ ] Theme toggle works end-to-end
- [ ] No remaining slate/purple classes in App.tsx
