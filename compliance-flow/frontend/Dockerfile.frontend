FROM node:22-alpine

WORKDIR /app

# Install dependencies first (cached layer)
COPY package.json package-lock.json ./
RUN npm ci

# Source files will be mounted via docker-compose volume
EXPOSE 5173

CMD ["npm", "run", "dev"]
